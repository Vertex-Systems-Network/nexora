# N1.0 Target Execution v2.3 — Maximum Closure Batch

Platform: `1.0.0-rc.38`.

This batch closes the remaining code-side release-integrity gaps without claiming target PASS:

1. read-only next-action planning (`n1-target-next-action` / master `--plan`);
2. real operator identity required before generating C4/C5/C6 kits;
3. centralized per-domain evidence freshness limits;
4. C5 HTTP/browser/Web-Vitals target URL consistency;
5. C6 HA evidence bound to the same HTTPS target as C5;
6. upgrade rehearsal source-version compatibility enforcement;
7. production release input freeze/revalidation to prevent mixed evidence/source artifacts;
8. independent production artifact verification of policy and current-host evidence hashes;
9. support capsule and closure dashboard expose the state-aware next action.

No lock acceptance, PHP extension mutation, dependency installation, operator observation, HA observation or production PASS is fabricated by these safeguards.
