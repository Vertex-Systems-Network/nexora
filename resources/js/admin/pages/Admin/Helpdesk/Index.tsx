import { Head } from "@inertiajs/react";import { AdminLayout } from "@admin/layout/AdminLayout";import { PageHeader } from "@admin/components/PageHeader";import { Icon } from "@admin/components/Icon";import { Badge,ButtonLink,Card } from "@nexora/admin-ui";import { HelpdeskNav } from "./_HelpdeskNav";
type Props={summary:{open:number;pending:number;urgent:number;unassigned:number;response_breaches:number;resolution_breaches:number};recent:Array<{id:string;reference:string;subject:string;status:string;priority:string;assignee:string|null;created_at:string|null}>};const human=(v:string)=>v.replace(/_/g,' ').replace(/\b\w/g,c=>c.toUpperCase());
export default function HelpdeskIndex({summary,recent}:Props){const stats=[['Open',summary.open,'inbox'],['Pending',summary.pending,'clock'],['Urgent',summary.urgent,'triangle-alert'],['Unassigned',summary.unassigned,'user-x'],['Response SLA breaches',summary.response_breaches,'timer-off'],['Resolution SLA breaches',summary.resolution_breaches,'circle-alert']] as const;return <AdminLayout><Head title="Helpdesk"/><PageHeader eyebrow="Customer support" title="Helpdesk" description="Provider-neutral ticketing with conversations, internal notes, assignments and first-response/resolution SLA foundations."/><HelpdeskNav current="/admin/helpdesk"/><div className="grid gap-3 sm:grid-cols-2 xl:grid-cols-3">{stats.map(([label,value,icon])=><Card key={label} className="p-5"><div className="flex items-center gap-3"><span className="grid h-10 w-10 place-items-center rounded-xl bg-[var(--nx-brand-soft)] text-[var(--nx-brand-600)]"><Icon name={icon} className="h-4 w-4"/></span><div><p className="text-xs text-[var(--nx-text-muted)]">{label}</p><p className="mt-1 text-xl font-semibold text-[var(--nx-text)]">{value}</p></div></div></Card>)}</div><Card className="mt-5 p-5 sm:p-6"><div className="flex items-center justify-between gap-3"><h2 className="font-semibold text-[var(--nx-text)]">Recent tickets</h2><ButtonLink href="/admin/helpdesk/tickets" variant="secondary" size="sm">View queue</ButtonLink></div><div className="mt-4 grid gap-2">{recent.length===0?<p className="py-8 text-center text-sm text-[var(--nx-text-muted)]">No support tickets yet.</p>:recent.map(t=><div key={t.id} className="flex flex-wrap items-center justify-between gap-3 rounded-xl border border-[var(--nx-border)] p-3"><div><ButtonLink href={`/admin/helpdesk/tickets/${t.id}`} variant="ghost" size="sm" className="h-auto p-0 font-semibold">{t.reference} · {t.subject}</ButtonLink><p className="mt-1 text-xs text-[var(--nx-text-muted)]">{t.assignee??'Unassigned'}</p></div><div className="flex gap-2"><Badge>{human(t.priority)}</Badge><Badge>{human(t.status)}</Badge></div></div>)}</div></Card></AdminLayout>}
