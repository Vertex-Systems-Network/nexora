version: 2

updates:
  # =========================================================
  # NPM / Node.js
  # =========================================================
  - package-ecosystem: "npm"
    directory: "/"

    schedule:
      interval: "weekly"
      day: "monday"
      time: "09:00"
      timezone: "Asia/Karachi"

    # Give newly released versions some time to stabilize.
    # Security updates are not delayed by cooldown.
    cooldown:
      semver-major-days: 14
      semver-minor-days: 7
      semver-patch-days: 3

    open-pull-requests-limit: 5

    groups:
      # Low-risk development updates can travel together.
      development-minor-patch:
        applies-to: "version-updates"
        dependency-type: "development"
        update-types:
          - "minor"
          - "patch"

      # Production patches are grouped; production minor and all major updates stay separate.
      production-patches:
        applies-to: "version-updates"
        dependency-type: "production"
        update-types:
          - "patch"

    commit-message:
      prefix: "chore"
      include: "scope"


  # =========================================================
  # Composer / PHP
  # =========================================================
  - package-ecosystem: "composer"
    directory: "/"

    schedule:
      interval: "weekly"
      day: "wednesday"
      time: "09:00"
      timezone: "Asia/Karachi"

    # Backend dependency changes remain separate for focused review.
    cooldown:
      default-days: 7

    open-pull-requests-limit: 5

    commit-message:
      prefix: "chore"
      include: "scope"


  # =========================================================
  # GitHub Actions
  # =========================================================
  - package-ecosystem: "github-actions"
    directory: "/"

    schedule:
      interval: "weekly"
      day: "friday"
      time: "09:00"
      timezone: "Asia/Karachi"

    cooldown:
      default-days: 7

    open-pull-requests-limit: 5

    commit-message:
      prefix: "chore"
      include: "scope"
